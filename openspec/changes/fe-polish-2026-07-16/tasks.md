## 1. Viewport adaptivity

- [x] 1.1 Deliver "Dynamic-viewport-height fallback on full-bleed sections": add a second `dvh` declaration (per the design decision "Always add `dvh` as a second declaration, never replace `vh`") immediately after each existing `vh` line on `.hero-section` (index.html), `.gantt-chart-container-rebuilt` (both cascading rules, index.html), and `.footer-cta` (index.html). Verify: scroll each section on an iOS Safari or Android Chrome device/emulator with the URL bar toggling — no visible height jump; `grep -n "dvh" index.html` shows a new line immediately following each of the three `vh` declarations.
- [x] 1.2 Deliver "Resize listeners are debounced": wrap the `resize`-triggered call to `updatePillRowFades` in `database.html` in a trailing-edge debounce (per the design decision "Debounce via `setTimeout`/`clearTimeout`, no new dependency", ~150ms). Verify: record a DevTools Performance trace while dragging the browser window edge — `updatePillRowFades` executes at most once per 150ms window during the drag, not once per resize event.
- [x] 1.3 Deliver "Minimum touch target size on interactive controls": increase vertical padding on `.nav-btn`, `.pill-btn`, `.page-btn` and add explicit `min-width`/`min-height: 44px` with flex centering on `.close-btn` (per the design decision "Touch targets grow via padding, not a fixed `min-height`"). Verify: DevTools computed-style panel shows a box height of at least 44px for each of the four selectors at both desktop and the existing 600px mobile breakpoint.
- [x] 1.4 Deliver "Reduced-motion preference is honored on both pages": add a `@media (prefers-reduced-motion: reduce)` block to `database.html` disabling the `.summary-bar`/`.modal-content` `fadeIn` animation and zeroing transitions on `.project-block`/`.pill-btn`/`.page-btn` (per the design decision "`database.html` reduced-motion handling mirrors `index.html`'s existing pattern"). Verify: enable `prefers-reduced-motion` in DevTools' rendering panel, trigger a search and open the modal — no animation plays and hover transitions are instant.

## 2. Accessible interactive controls

- [x] 2.1 Deliver "Modal is fully keyboard-operable": change `<span class="close-btn" onclick="closeModal(true)">` to a semantic `<button type="button" class="close-btn" aria-label="關閉">`, and add a single `document`-level `keydown` listener that calls `closeModal(true)` when `e.key === 'Escape'` and the modal is currently visible (per the design decision "Modal close becomes a real `<button>`; Escape handled at `document` level"). Verify: manual keyboard-only pass — `Tab` reaches the close button, `Enter`/`Space` activates it, and `Escape` closes the modal regardless of current focus.
- [x] 2.2 Deliver "Pagination controls expose accessible names and current state": add `aria-label="上一頁"`/`aria-label="下一頁"` to the pagination prev/next buttons and `aria-current="page"` to the button matching `currentPage`, set inside the same template literal that assigns the `.active` class (per the design decision "ARIA state is toggled in the same render functions that toggle the `.active` CSS class"). Verify: inspect the rendered DOM after changing pages — exactly one `.page-btn` per page has `aria-current="page"`, and the prev/next buttons have their `aria-label`.
- [x] 2.3 Deliver "Filter pills expose pressed state to assistive technology": set `aria-pressed="true"`/`"false"` on each `.pill-btn` inside `renderThemePills`/`renderMinistryPills` at the same point the `.active` class is assigned. Verify: select a theme pill and inspect the DOM — the selected pill has `aria-pressed="true"`, all sibling pills in that group have `aria-pressed="false"`.
- [x] 2.4 Deliver "Visible focus indication on interactive controls": add a `:focus-visible` rule to each of `.nav-btn`, `.pill-btn`, `.page-btn` using the `--np-seal-red`/`--np-seal-red-bright` tokens (per the design decision "`:focus-visible` is defined per component class, not as a global catch-all"). Verify: force `:focus-visible` state in DevTools on one instance of each selector — a visible ring distinct from the hover style appears.
- [x] 2.5 Deliver "Pages provide a skip-to-content link and current-page nav indication": add a `.skip-link` (visually hidden until `:focus`) as the first child of `<body>` in both `index.html` and `database.html` pointing to `#main`, and add a static `aria-current="page"` attribute alongside each file's existing hardcoded `.nav-active` link (per the design decision "Skip link and nav `aria-current` are static HTML, not JS-driven"). Verify: fresh page load, press `Tab` once — the skip link becomes visible and activating it moves focus to `#main`; `grep -n 'aria-current' index.html database.html` shows one match per file.

## 3. Social / SEO metadata

- [x] 3.1 Resolve the design's Open Question by confirming the final published base URL/domain for these two pages with the user, before substituting real values into any absolute-URL field added in tasks 3.2-3.5. Verify: user has explicitly provided the base URL in this conversation, or explicitly confirmed a `PUBLISH_BASE_URL` placeholder comment is acceptable to ship temporarily — not silently guessed.
- [x] 3.2 Deliver "Pages declare description and Open Graph metadata": add `<meta name="description">` and `og:title`/`og:description`/`og:image`/`og:url`/`og:type` tags to both `index.html` and `database.html`, using the base URL resolved in task 3.1 for the absolute `og:image`/`og:url` fields. Verify: paste each page's URL into a platform OG-tag debugger (or `curl` + manual inspection) — title, description, and image all populate correctly.
- [x] 3.3 Deliver "Pages declare Twitter Card metadata": add `twitter:card`, `twitter:title`, `twitter:description`, `twitter:image` tags to both pages. Verify: `grep -n 'twitter:' index.html database.html` shows all four tags present in each file.
- [x] 3.4 Deliver "Pages declare a canonical URL": add `<link rel="canonical">` to both pages using the base URL from task 3.1. Verify: `grep -n 'canonical' index.html database.html` shows one absolute-URL canonical link per file.
- [x] 3.5 Deliver "Pages declare structured data": add a `<script type="application/ld+json">` block to each page (per the design decision "JSON-LD and social meta are structured now; absolute URLs are parameterized") — `NewsArticle` type for `index.html`, `Dataset` or `WebPage` type for `database.html` — with `headline`/`name` and `description` matching each page's actual visible content. Verify: paste each JSON-LD block into a structured-data validator — no schema errors, and `@type`/`headline`/`description` match the live page content.

## 4. Browser compatibility

- [x] 4.1 Deliver "matchMedia calls are guarded against unsupported environments": wrap both `window.matchMedia(...)` call sites in `index.html` (the `prefers-reduced-motion` check and the chapter-3 mobile/desktop sync check) in a `typeof window.matchMedia === 'function'` guard, falling back to motion-enabled and the desktop branch respectively when unavailable. Verify: in DevTools console, temporarily delete `window.matchMedia` and reload — no thrown JavaScript error at either call site, and each feature falls back to its defined default.
- [x] 4.2 Deliver "Image loading hints reflect visual priority": add a `<link rel="preload" as="image" fetchpriority="high">` hint for the hero background image in `index.html`'s `<head>`, and add `loading="lazy"` to the three below-fold historical photos (`budget96001.jpg`, `budget96002.jpg`, `budget003.jpg`). Verify: DevTools Network panel shows the hero image requested at high priority immediately on load, and the three photo requests deferred until they approach the viewport.
