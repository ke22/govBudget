## 1. Responsive Hero

- [x] 1.1 Deliver **Responsive hero uses live semantic text and one clean shared asset** by editing the existing mobile collage into `115MoneyDemoB-main/hero-collage-mobile-clean.jpg` without required text while preserving the report subjects and newspaper texture; verify the source image contains no title/subtitle information and inspect its crops at 390 x 844, 768 x 1024, and 820 x 1180 before wiring it into the page.
- [x] 1.2 Implement **Compact and intermediate heroes use live editorial content** in `115MoneyDemoB-main/index-v2.html`: expose a visible semantic `h1`, subtitle, live overdue stamp, dek, and scroll affordance through 1024 CSS pixels while keeping the collage decorative and a deep-canvas image fallback; verify at 390 x 844 and 768 x 1024 with the image available and deliberately unavailable that all required content remains readable and appears exactly once in the accessibility tree.
- [x] 1.3 Implement **Hero layout uses content-driven height**, **Responsive composition uses three layout bands**, and **RWD uses three layout bands and conditional bespoke composition**: use normal-flow compact (up to 640), intermediate (641-1024), and preserved wide (1025+) layouts with no device sniffing or text-on-text fixed offsets; verify boundary widths 640, 641, 1024, and 1025 plus the full matrix for overlap and horizontal overflow. If CSS framing and spacing still fail at 768 x 1024 or 820 x 1180, stop before creating separate tablet markup/artwork and report the exact viewport, crop, and collision for user art-direction approval.

## 2. Chapter 2 Exit Lifecycle

- [x] 2.1 Implement **Chapter 2 history visual exits with the final story card** and **Chapter 2 visuals fade using final card geometry** inside the existing requestAnimationFrame scroll path: derive exit progress from the visible final `.story-card` top, map 55vh to progress 0 and 35vh to progress 1, and drive a dedicated stage exit state without changing step selection; verify opacity values at 60vh, 55vh, 45vh, 35vh, and 25vh against the specification table and confirm the hidden stage cannot receive pointer events.
- [x] 2.2 Implement **Chapter 2 visual cannot cover Chapter 3** and **Exit state is reversible and motion-aware**: force the stage hidden when Chapter 3 reaches the viewport bottom, recompute state while reverse-scrolling, provide CSS containment when JavaScript is absent, and switch without interpolation at 45vh under reduced motion; verify normal scrolling, a large forward jump, reverse scrolling from Chapter 3, JavaScript-disabled fallback behavior, and `prefers-reduced-motion: reduce`.

## 3. Editorial Hierarchy

- [x] 3.1 Implement **Critical Chinese headings wrap at authored phrase boundaries** and **Chinese headings wrap at authored phrase boundaries** by marking the two documented headings with nonbreaking phrase spans plus optional inter-phrase breaks, assigning 1.75rem/2rem/2.5rem sizes by layout band, and preserving body size; verify the exact expected phrase pairs at 390 x 844 and confirm no phrase overflows at every matrix viewport.
- [x] 3.2 Implement **Editorial typography uses five roles** by defining report-display, chapter-heading, chart/panel-heading, body, and caption/label tokens or classes and migrating affected report content away from conflicting one-off declarations; verify a computed-style inventory contains only those five roles and that body copy retains its established size at 640, 641, 1024, and 1025 CSS pixels.
- [x] 3.3 Implement **UI colors use six non-data roles**, **Affected typography and color declarations are role-based**, and **Typography and color use editorial roles while charts retain semantic palette**: map non-data UI to deep canvas, paper surface, primary ink, inverse ink, seal-red accent, and muted text/border; remove unrelated yellow/teal/blue uses and conflicting inline color/type declarations while preserving the five chart category tokens; verify with a source color audit plus screenshots of headings, navigation, controls, story cards, and every chart category.

## 4. Chart Viewport Clearance

- [x] 4.1 Implement **Chart stages share header-aware top clearance** and **Chart stages share header-aware viewport clearance** by introducing one clearance token containing fixed-header height, top safe-area inset, and a 24px gap, then applying it to the complete Chapter 2, Chapter 3, and Chapter 4 panels without moving internal axes or data marks; verify measured topmost chart content is at least 24 CSS pixels below the header and plot/label alignment is unchanged at all matrix viewports.
- [x] 4.2 Implement **Short wide viewports receive additional clearance** and **Chart panels fit remaining viewport height**: add 16px clearance for wide viewports no taller than 900 CSS pixels and cap each panel to remaining viewport space with a 24px bottom gap; verify at 1366 x 768 that `96年度`, bottom axes, legends, and controls are visible, aligned, unclipped, and do not create an internal chart scrollbar, then repeat bounds checks for all Chapter 2-4 panels at 390 x 844.

## 5. Cross-Device Verification and Scope

- [x] 5.1 Capture browser screenshots and run DOM overflow checks at 390 x 844, 430 x 932, 768 x 1024, 820 x 1180, 1024 x 768, 1366 x 768, and 1440 x 900; verify hero framing, semantic heading breaks, story-card/chart separation, Chapter 2 exit, Chapter 3 entry, chart bounds, and non-overlap across the entire matrix.
- [x] 5.2 Scroll the Hero-to-Chapter-3 path forward and backward at 390 x 844, 768 x 1024, and 1366 x 768, including a large scroll jump and reduced-motion mode; verify no stale fixed layer, text/image collision, blank stage, interaction blockage, or chapter-title occlusion occurs and record whether the shared intermediate composition passed or requires user-approved tablet art direction.
- [x] 5.3 Confirm scope and static-host compatibility: inspect the change diff to verify only `115MoneyDemoB-main/index-v2.html` and `115MoneyDemoB-main/hero-collage-mobile-clean.jpg` changed, `115MoneyDemoB-main/index.html` and database pages remain untouched, no runtime dependency or build step was added, and all current budget data, chart category semantics, and story step order are unchanged.

## 6. Header-Compensated Responsive Foundation

- [x] 6.1 Deliver **Viewport-sized regions subtract the header once** and the updated **Hero layout uses content-driven height** contract in `115MoneyDemoB-main/index-v2.html`: make compact/intermediate Hero minimum height equal to the `100svh` space remaining below the shared header while retaining content growth, remove duplicate header compensation, and verify computed Hero/header bounds at 390 x 844, 768 x 1024, 1024 x 768, and 1440 x 900.
- [x] 6.2 Extend **Chart stages share header-aware top clearance** so Chapter 2 and Chapter 3 sticky visual-stage outer heights fit the small viewport space remaining below the header without double subtraction; verify the complete panel, top label, bottom axis, and story reading zone at 390 x 844 and 1366 x 768.
- [x] 6.3 Implement **Fixed utility controls do not cover hero content** by keeping `.back-to-hero-btn` hidden and non-hit-testable while the Hero is active, revealing it after the reader leaves the Hero, and verifying geometric disjointness from title, subtitle, stamp, dek, and scroll affordance at 390 x 844 and 430 x 932 plus keyboard access after Chapter 1 begins.

## 7. Fluid Editorial Typography

- [x] 7.1 Update **Editorial typography uses five roles** and **Chinese body and annotation text retain readable minimums** with bounded `clamp()` role tokens: keep primary Chinese body copy at or above 16px, chart annotations/captions at or above 12px, small text at regular/medium weight, and verify computed sizes and weights at 390, 640, 768, 1024, 1025, and 1440 CSS pixels without introducing a sixth role.
- [x] 7.2 Complete **Affected typography and color declarations are role-based** by migrating affected navigation, stamp, timeline, chart-label, and utility-control declarations from direct sizes and legacy `--text-main`/`--text-muted` aliases to the existing five type roles and six UI roles; verify with a source audit that chart category colors remain data-only and the existing editorial visual identity is unchanged, satisfying **Refinement preserves the current editorial identity**.

## 8. Responsive Storytelling Architecture

- [x] 8.1 Implement **Storytelling layout and motion use three tiers** and **Responsive behavior follows three motion tiers**: compact layouts use normal-flow single-column scenes with no readability-gating reveal, intermediate layouts retain simplified chart-state changes with shorter sticky duration and smaller transforms, and wide layouts begin at 1025px with the complete Chapter 1 timeline and Chapter 2/3 scrollytelling; verify boundaries at 640, 641, 1024, and 1025 CSS pixels.
- [x] 8.2 Deliver **CSS owns layout and JavaScript owns state** by moving primary card widths, flow positions, sticky boundaries, and layout transforms out of scroll-time inline assignments while retaining semantic step classes or bounded CSS variables; verify the full page remains correctly ordered and readable with JavaScript disabled and that scroll handlers perform no primary layout writes.
- [x] 8.3 Deliver **Scroll runways derive from authored scenes** and **Scroll runways follow scene count and reading rhythm** by replacing chapter-wide magic heights such as `850vh` with documented scene-count/read-interval calculations, using content-driven flow on compact layouts and shorter intervals on intermediate layouts; verify adding or removing one test scene changes only its chapter runway contract and does not require an unrelated fixed-height edit.
- [x] 8.4 Implement **Reduced-motion and no-JavaScript states are directly readable** and **Reduced motion and no-JavaScript states remain readable**: make required cards/labels visible by default, remove parallax-like displacement and interpolated reveal transforms under reduced motion, preserve immediate essential chart-state switches, and verify Hero-to-Chapter-3 reading order with JavaScript disabled plus `prefers-reduced-motion: reduce` at 390 x 844, 768 x 1024, and 1366 x 768.

## 9. Refinement Verification

- [x] 9.1 Re-run the seven-viewport matrix at 390 x 844, 430 x 932, 768 x 1024, 820 x 1180, 1024 x 768, 1366 x 768, and 1440 x 900 after tasks 6–8: capture screenshots and DOM bounds, scroll forward and backward through Hero-to-Chapter-3, confirm no overlap, clipping, horizontal overflow, stale fixed layer, or console error, and confirm compact/intermediate/wide behavior matches the updated contract without changing copy, data, images, chapter order, or the core editorial/scrollytelling concepts.
