## 1. Timeline Card and Node Scroll-Driven Reveal CSS

- [x] 1.1 Replace the forced static display CSS on timeline cards and nodes with scroll-driven reveal styles. In `115MoneyDemoB-main/index.html`, locate the CSS rule `.timeline-row.item-card .story-card` (currently near line 635) that sets `pointer-events: auto !important; opacity: 1 !important; transform: none !important;` and replace it with the progressive reveal CSS: `.js .center-timeline-container .timeline-row.item-card .story-card` starts hidden (`opacity: 0 !important; pointer-events: none !important; transform: translateY(18px) rotate(-0.4deg) !important; transition: opacity 320ms ease, transform 420ms cubic-bezier(0.16, 1, 0.3, 1) !important;`) and `.js .center-timeline-container .timeline-row.item-card.timeline-card-revealed .story-card` becomes visible (`opacity: 1 !important; pointer-events: auto !important; transform: translateY(0) rotate(-0.4deg) !important;`). Add equivalent rules for `.js .center-timeline-container .timeline-row.item-node .timeline-block-node` (hidden: `opacity: 0 !important; transform: translateY(18px) !important;` with same transition; revealed via `.timeline-node-revealed`: `opacity: 1 !important; transform: translateY(0) !important;`). This fulfills the "Timeline card reveal leads the growing line by a fixed offset" requirement. Verify by opening the page, scrolling through Chapter 1, and confirming cards/nodes start invisible and fade in as the red line passes them. Also add the `.js` class to the `<html>` element via a small inline script `document.documentElement.classList.add('js');` at the top of the body, if not already present, so the progressive reveal only activates when JS is available.

## 2. Timeline Red Line Fill Logic Enhancement

- [x] 2.1 Extend the `updateTimelineMainLineFill()` JavaScript function to compute the red line fill tip's viewport Y coordinate and toggle reveal classes on timeline cards and nodes. After computing `percent`, calculate `fillTipViewportY = rect.top + (percent / 100) * rect.height`. Then iterate over all `.timeline-row.item-card` and `.timeline-row.item-node` elements within `#center-timeline-container` using `container.querySelectorAll('.timeline-row.item-card, .timeline-row.item-node')`. For each row, get its `getBoundingClientRect()` and toggle `.timeline-card-revealed` (for `item-card`) or `.timeline-node-revealed` (for `item-node`) when `fillTipViewportY >= rowRect.top + 40`. This implements the 40-pixel lead offset from the "Timeline card reveal leads the growing line by a fixed offset" requirement. Verify by scrolling forward: cards appear after the line passes them. Scroll backward: cards disappear when the line retracts past them.

## 3. Timeline Terminus Mark

- [x] 3.1 Add the terminus mark HTML element and CSS styles. In `115MoneyDemoB-main/index.html`, insert `<div class="timeline-terminus-mark" id="timeline-terminus-mark" aria-hidden="true"></div>` inside `.center-timeline-container` as a sibling after the `.center-main-line` closing `</div>` (not as a child of `.center-main-line`). Add CSS: `.timeline-terminus-mark` positioned `absolute; left: 50%; bottom: 0; transform: translate(-50%, 50%); z-index: 2; width: 28px; height: 28px; border-radius: 50%; background: var(--ui-accent); color: var(--ui-paper); display: flex; align-items: center; justify-content: center; font-weight: 700; font-size: 0.95rem; box-shadow: 0 2px 6px rgba(27, 23, 18, 0.25); animation: timeline-terminus-pulse 1.8s ease-in-out infinite;`. Add `::before { content: '?'; }`. Add `.timeline-terminus-mark.timeline-complete { animation: none; transform: translate(-50%, 50%) scale(1); }` and `.timeline-terminus-mark.timeline-complete::before { content: '\2193'; }`. Add `@keyframes timeline-terminus-pulse { 0%, 100% { opacity: 0.55; transform: translate(-50%, 50%) scale(0.92); } 50% { opacity: 1; transform: translate(-50%, 50%) scale(1.08); } }`. This fulfills the "Timeline terminal marker reflects completion state" requirement. Verify: a pulsing "?" appears at the bottom of the timeline.

- [x] 3.2 Add JavaScript to toggle the terminus mark's completion state. In the `updateTimelineMainLineFill()` function, after computing `percent`, add: `const terminusMark = document.getElementById('timeline-terminus-mark'); if (terminusMark) { terminusMark.classList.toggle('timeline-complete', percent >= 100); }`. Verify: scroll to the bottom of the Chapter 1 timeline so the red line reaches 100% — the "?" stops pulsing and changes to "↓". Scroll back up — it reverts to a pulsing "?".

## 4. Chapter 2 Story Card Readability Fix

- [x] 4.1 Adjust the opacity calculation thresholds in the Chapter 2 scroll handler so that "History story cards remain readable until past 50% viewport height". In `115MoneyDemoB-main/index.html`, locate the `updateChapter2Stage()` function (or the relevant scroll handler that computes story card opacity). Find the lines that compute the opacity fade start and end thresholds (currently using values like `viewportHeight * 0.55` for start and `viewportHeight * 0.35` for end, or similar). Change the start threshold to `viewportHeight * 0.50` (cards begin fading when their top edge is at 50% viewport height) and the end threshold to `viewportHeight * 0.30` (cards reach opacity 0 when their top edge is at 30% viewport height). Verify by scrolling through the Chapter 2 history section: the two story cards (96年度總預算 card and 89年度 card) remain fully readable (opacity >= 0.85) when their top edge is at 50% viewport height.

## 5. Pyramid Chart Entrance Animation

- [x] 5.1 Enable the "Pyramid chart bars animate from zero to target width on scroll entry" by removing inline override styles and ensuring CSS transitions work. In `115MoneyDemoB-main/index.html`, locate the pyramid chart HTML elements (`.dynamic-chart-box`, `.dash-container-box`, individual `.pyramid-align-row` elements) and remove any inline `style="opacity: 1; visibility: visible !important; transform: none !important;"` attributes that override the CSS animation. Ensure the `.bar-rect` elements have `width: 0` by default in CSS and only expand to their target width when the parent `.dynamic-chart-box` has the `.visible-box` class. The existing CSS rule `.bar-rect { transition: width 1s ease-out; }` handles the transition. Add staggered `transition-delay` to each `.pyramid-align-row:nth-child(N) .bar-rect`: 0ms, 50ms, 100ms, 150ms, 200ms for rows 1–5. Verify by scrolling to the 718億先行動支方案 section: bars grow from zero to their target width with a sequential stagger when the chart enters the viewport.

## 6. Primary Red Contrast Fix

- [x] 6.1 Fix the "Primary red achieves readable contrast on dark backgrounds" by updating the mobile Chapter 1 timeline line gradient. In `115MoneyDemoB-main/index.html`, find the CSS rule `#chapter-1 .local-scroll-content::before` (the mobile vertical timeline line) that uses `background: linear-gradient(to bottom, var(--primary) 0%, ...)`. Replace `var(--primary)` with `var(--np-seal-red-bright)` so the gradient start color is `#E2564A` instead of `#A5271E`. Also update `#chapter-1 .mobile-node-item` border from `border: 2px solid var(--primary)` to `border: 2px solid var(--np-seal-red-bright)`. Do NOT change the CTA button (`cta-btn`) background since white-on-dark-red already passes contrast. Verify by viewing the mobile Chapter 1 section: the timeline line and node borders use the brighter red that is clearly visible against the dark background.

## 7. Footer Visual Distinction

- [x] 7.1 Make the footer visually distinct per the "Footer is visually distinct from the content section above" requirement. In `115MoneyDemoB-main/index.html`, locate the `<footer>` element (inline styled with `background: radial-gradient(...), var(--bg-deep)`). Change the background base from `var(--bg-deep)` to `#1E1410` (warm dark charcoal) and add a `border-top: 1px solid rgba(237, 230, 211, 0.12);` to create a visible separator. Keep the radial dot texture overlay. Verify by scrolling to the bottom: the footer area has a noticeably warmer, distinct tone from the `.footer-cta` section above.

## 8. Mobile Scroll Indicator Centering and Animation

- [x] 8.1 Add the mobile-specific scroll indicator overrides per the "Hero scroll indicator stays fully visible on phone" requirement. In `115MoneyDemoB-main/index.html`, add a `@media (max-width: 640px)` block containing: `.scroll-indicator { bottom: 90px; animation: bounce-mobile 2s infinite; }` and `.scroll-indicator::after { height: 26px; }`. Add `@keyframes bounce-mobile { 0%, 20%, 50%, 80%, 100% { transform: translate(-50%, 0); } 40% { transform: translate(-50%, -6px); } 60% { transform: translate(-50%, -3px); } }`. This lifts the indicator above the torn clip-path edge, shortens the tail line, and uses a dampened bounce. Verify on a <= 640px viewport: "向下捲動探索" is horizontally centered, fully visible (not clipped), and bounces with reduced amplitude.

## 9. Mobile Red Line Scroll Animation

- [x] 9.1 Ensure the "Mobile red timeline line animates on scroll" by NOT forcing `height: 100% !important` on mobile. Check `115MoneyDemoB-main/index.html` for any `@media (max-width: 640px)` rule that sets `.center-main-line-fill { height: 100% !important; }`. If such a rule exists, remove it so the JavaScript `updateTimelineMainLineFill()` drives the fill height on mobile identically to desktop. If no such rule exists, confirm that the existing JS scroll handler works on mobile viewports. Verify on a <= 640px viewport: the Chapter 1 red line grows from 0% to 100% as the user scrolls through the timeline, matching desktop behavior.
