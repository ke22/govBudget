## 1. Chapter 2 card 1: empty chart frame distinct from card 2's axis highlight

- [x] 1.1 Implement "Chapter 2 card 1 shows an empty chart frame distinct from card 2's axis highlight" per the "Chapter 2 card 1: add a gray/empty frame state gated on `.rebuilt-step-1`, deferring axis opacity to `.rebuilt-step-2` unchanged" decision: add `ganttBox.classList.add('box-visible-rebuilt-now')` to the `currentStep === 1` branch of `updateChapter2Stage()`, and add a `.rebuilt-step-1`-gated CSS rule showing the chart panel frame and grid in a muted state, without touching the existing `.rebuilt-step-2`-gated Y-axis/X-axis opacity rules. Verification: source review confirms `.rebuilt-step-1` gains a panel-frame/grid rule and the existing axis-opacity rules remain gated to `.rebuilt-step-2` only.
- [x] 1.2 Verify "Card 1 shows the frame without axis emphasis" and "Card 2 adds the axis highlight" at 1366x768: scrolling to card 1 shows the muted panel+grid with no axis highlight, and scrolling to card 2 adds the X-axis/Y-axis highlight on top of the already-visible frame. Verification: scroll-through capture (or computed opacity read) at each card shows the expected incremental state.

## 2. Chapter 3 grey total-execution bar: distinct reveal

- [x] 2.1 Implement "Chapter 3's grey total-execution bar has a distinct reveal from the colored bars" per the "`#ch3-bar-total-grey`: an explicit reveal rule with its own duration/easing, not a shared one" decision: add a `#ch3-bar-total-grey`-scoped transition override (distinct duration/easing from the shared `.ch3-single-bar-layer` transition) without changing the shared rule or the `.ch3-step-1`/`.ch3-step-2` gating logic. Verification: source review confirms `#ch3-bar-total-grey` has its own transition declaration distinct from `.ch3-single-bar-layer`'s shared value, and no other bar's CSS is changed.
- [x] 2.2 Verify "Grey bar reveal reads as distinct" at 1366x768: scrolling forward and reverse through Chapter 3's step 1-to-2 transition shows the grey bar's reveal visibly differing in timing/easing from the colored bars' reveal. Verification: scroll-through capture (or computed transition-duration read) at the reveal step shows the grey bar's transition value differs from the colored bars'.

## 3. Chapter 3 last card and chart exit coordination (verification only)

- [x] 3.1 Verify "Chapter 3's last card and chart move together during exit", per the design decision Chapter 3 last card/chart "scroll up together": already satisfied by the existing sticky+transform mechanism — verify, don't reimplement: confirm no code change is needed by re-reading `updateChapter3Bars()`'s existing `--ch3-exit-offset` computation and its application to `#ch3-sticky-box-wrapper`'s transform. Verification: source review confirms the offset is capped at the wrapper's own height and recomputed from live geometry every frame, with no separate/independent progress calculation.
- [x] 3.2 Verify "Chart follows the last card during forward scroll" and "Reverse scroll restores the chart's position symmetrically" at 1366x768 and 1024x768, including one fast scroll-jump test: the sticky chart box visually stays with the last story card with no overlap in either direction, and no stale offset persists after a fast forward-then-backward jump. Verification: forward, reverse, and scroll-jump capture at both viewports shows the chart and last card never visually overlapping and the offset always matching current card position.

## 4. Chapter 3 sticky box: vertical centering at desktop width

- [x] 4.1 Implement "Vertical distribution of content within cleared chart space is a presentation choice" per the "`#ch3-sticky-box-wrapper` vertical centering: override the actual cascade winner (`align-items: flex-start !important` at line ~3024), desktop-only, keep the top anchor" decision: add a `@media (min-width: 1025px)` rule for `#ch3-sticky-box-wrapper` setting `align-items: center !important` with equal-or-higher specificity placed after the existing unconditional `flex-start` rule, without changing `top`, `height`, or the clearance-enforcing padding. Verification: source review confirms the new rule is scoped to `min-width: 1025px`, does not touch `top`/`height`/padding, and is positioned to win the cascade over the unconditional rule at that width.
- [x] 4.2 Verify "Centering content within the cleared band remains compliant" at 1366x768 and 1440x900: the chart content visibly centers within the header-safe band, the topmost chart content (the `96年度` label) remains at least 24 CSS pixels below the fixed header, and the bottommost chart content remains inside the viewport. Verification: `getBoundingClientRect()` read (or visual capture) at both viewports confirms both clearance minimums still hold with the new centered alignment.
- [x] 4.3 Verify no regression below `1025px`: at 768x1024 and 390x844, `#ch3-sticky-box-wrapper`'s alignment remains top-anchored (`flex-start`) exactly as before this change. Verification: computed-style read at both viewports confirms `align-items: flex-start` is still in effect.

## 5. Chapter 3 intro reveal gating

- [x] 5.1 Implement "Chapter 3 intro reveal is gated on Chapter 2's exit completion" per the "Chapter 3 intro reveal: reuse the already-computed `history-exit-complete` state, not a coarser stage-lifecycle signal" decision: add a reveal class/transition to `#chapter-3-rebuilt .chapter-intro` (default hidden/offset, `.js`-gated) and toggle a new revealed class from `updateChapter2Stage()` based on `stage.classList.contains('history-exit-complete')`. Verification: source review confirms the new class is read from the existing `history-exit-complete` state, not a new geometry computation or the `stage-active-rebuilt-now` class.
- [x] 5.2 Verify "Intro reveals once the exit fade completes" and "Intro is not revealed before the exit fade completes" at 1366x768: forward-scrolling through Chapter 2's ending shows the intro staying hidden until `history-exit-complete` is set, then fading in; reverse-scrolling shows it fading back out. Verification: scroll-through capture (or class-state read) shows the intro's revealed class toggling exactly with `history-exit-complete`.
- [x] 5.3 Verify "Reduced motion and no-JavaScript show the intro immediately", consistent with `responsive-storytelling-motion`'s existing reduced-motion/no-JS requirements: with `prefers-reduced-motion: reduce` enabled or JavaScript disabled, the intro is visible immediately with no reveal delay. Verification: page load under both conditions shows the intro text visible without waiting for any scroll-driven state.

## 6. Cross-cutting regression check

- [x] 6.1 Re-run the seven-viewport matrix (390x844, 430x932, 768x1024, 820x1180, 1024x768, 1366x768, 1440x900) forward/reverse scroll check already used for `stabilize-responsive-story-layout` and `fix-responsive-story-acceptance-gaps`, confirming no new overlap, clipping, or horizontal overflow was introduced by this change's Chapter 2/3 CSS/JS edits. Verification: DOM overflow checks and scroll-through capture at all seven viewports show no regression relative to the prior baseline.
