## 1. Setup: duplicate files before any edit

- [x] 1.1 Follow the duplicate-first workflow for index-v2.html and database-v2.html: create `115MoneyDemoB-main/index-v2.html` and `115MoneyDemoB-main/database-v2.html` as byte-for-byte copies of the current `115MoneyDemoB-main/index.html` and `115MoneyDemoB-main/database.html`. Verify with `diff 115MoneyDemoB-main/index.html 115MoneyDemoB-main/index-v2.html` and `diff 115MoneyDemoB-main/database.html 115MoneyDemoB-main/database-v2.html` — both must show zero output. No other task in this list may start until this task is done.

## 2. 報導頁：Header 與時間軸

- [x] 2.1 Swap the header nav button order in both `index-v2.html` and `database-v2.html` so 完整資料庫 comes before 總預算案專頁 (new order: 年度深度報導 → 完整資料庫 → 總預算案專頁). Verify by reading the rendered nav link order in both files' `<nav>` markup.
- [x] 2.2 Remove the 逾期/警示/付委 circular badge (the `::after` content) from `.timeline-block-node.node-danger/-warning/-success` in `index-v2.html`, while keeping the existing colored left-border card styling intact. Verify visually that the timeline cards still show their colored border but no circular badge overlapping the card.

## 3. 報導頁：封面 Hero 位置

- [x] 3.1 Fix the hero title/dek-panel/stamp overlap in `index-v2.html`: adjust `.hero-stamp-wrap`'s positioning so it no longer overlaps `.hero-dek-panel` and reads as closer to horizontal/vertical center across common desktop and laptop aspect ratios (16:9, 16:10, ultrawide), not just the current single 640px breakpoint. Verify with a live browser check at at least two distinct desktop-class viewport aspect ratios (e.g. 1920x1080 and 1440x900) confirming no visual overlap between the stamp circle and the dek-panel box.

## 4. 報導頁：第二章視差滾動同步與動態

- [x] 4.1 Implement the "Chapter-2 card→chart sync uses a tighter IntersectionObserver rootMargin" decision in `index-v2.html`: change the observer's `rootMargin` bottom value from `-20%` to `-8%` (keeping the top value at `-10%`). Verify by live-scrolling Chapter 2 and confirming the Gantt chart's step visibly updates noticeably sooner after card 2's text ("中央政府總預算案原則上應於前一年度底完成審議...") enters the reading zone, compared to the current behavior, while bars still do not grow before their card is on-screen.
- [x] 4.2 Nudge `.gantt-fixed-stage-rebuilt`/`.gantt-chart-container-rebuilt` desktop padding down slightly in `index-v2.html` so the 96年度 axis label is no longer crowded against the fixed header on laptop-height screens. Verify visually at a ~900px-tall desktop viewport that there is visible clearance between the header and the 96年度 axis label.
- [x] 4.3 Add the missing `.rebuilt-step-1` mobile visibility-toggle rule in `index-v2.html`'s `@media (max-width: 968px)` block, following the exact same pattern as the existing `.rebuilt-step-2` through `.rebuilt-step-9` rules, so that Chapter 2's card 1 ("回顧歷年審查歷程，在預算執行年度開始前就完成審查的例子並不常見。") becomes visible when `#chapter-2-rebuilt` has class `rebuilt-step-1`. Verify via `getComputedStyle` on the step-1 card at a mobile viewport showing `opacity: 1; visibility: visible` when that step is active.
- [x] 4.4 Implement "Mobile Chapter-2/3 cards get a translate-based slide instead of an opacity fade" in `index-v2.html`: replace the mobile `.gantt-card-rebuilt` opacity/visibility toggle with a `transform: translateY(...)` + `opacity` transition (outgoing card translates up and fades out, incoming card translates in from below and fades in), satisfying the "Mobile Chapter-2/3 story cards slide bottom-to-top instead of fading in place" requirement for every step including the transition into and out of step 9. Verify by live-scrolling Chapter 2/3 on a mobile viewport and confirming cards visibly travel bottom-to-top (not fade in place at center), that the step-9 card slides up and off the top of the screen instead of vanishing instantly with its photo, and that the transitioning card never visually overlaps the pinned chart band.
- [x] 4.5 Verify inactive/outgoing cards do not intercept pointer events or expose themselves to assistive technology as visible, per the "only one card is interactive/visible at a time" scenario — confirm via `getComputedStyle` that any card in its hidden state still resolves to `opacity: 0` and is excluded from the accessibility tree (e.g. via `visibility: hidden` at rest or `aria-hidden`/`pointer-events: none` while transitioning).
- [x] 4.6 Scale down the mobile Chapter-2 Gantt chart by roughly 5% in `index-v2.html`'s `@media (max-width: 968px)` block so the chart top is no longer clipped by / crowded against the fixed header, while keeping every Gantt row and axis label legible. Behavior delivered: on a mobile viewport the full chart (including the topmost 96年度 axis row) is visible below the header with no clipping. Implement via a proportional shrink of the mobile chart (e.g. `transform: scale(0.95)` with `transform-origin: top center`, or an equivalent proportional reduction of the mobile chart's dimensions/paddings) — do NOT alter the desktop chart sizing. If a 5% shrink alone does not fully clear the header, also add mobile top clearance so nothing is clipped. Verify at a 390–430px-wide viewport that the 96年度 row is fully visible (its `getBoundingClientRect().top` is greater than the header's bottom edge) and no chart content is cut off at the top.

## 5. 報導頁：文案更新

- [x] 5.1 Update Chapter-2 card 3 text in `index-v2.html`: change 立時 to 歷時. Verify the rendered card text reads "...方塊的顏色則代表執政黨。" with 歷時 in place of 立時.
- [x] 5.2 Update Chapter-2 card 4 text in `index-v2.html` to "紅色的虛線是法律規定預算審理完成時間，淺灰色的區塊則代表預算應該開始執行的時間". Verify the rendered card text matches exactly, including 淺灰色 (not 深灰色).
- [x] 5.3 Update Chapter-2 card 5 text in `index-v2.html` to "蔡英文執政8年間只有1次（113年度）在時限內完成審議，更早的馬英九執政時期也僅有2次（101年度及105年度），大部分都是到隔年2月前才完成審議。". Verify the rendered card text matches exactly.
- [x] 5.4 Update the Chapter-2 closing paragraph in `index-v2.html` to "這些項目因為預算遲未審查而無法使用，雖然補助等經費可以在總預算三讀後回溯發放，但凡涉及工程等費用，可能因為執行期不足而導致經費無法發揮使用，最終剩餘沒有執行的錢仍須回歸國庫，不能留到明年繼續沿用。". Verify the rendered paragraph matches exactly.
- [x] 5.5 Update the Chapter-3 first paragraph in `index-v2.html` to "前述提到，受預算法規定，在總預算未審議前，新興資本支出及新增計畫原則上不能動用。". Verify the rendered paragraph matches exactly.
- [x] 5.6 Reverse the footer CTA order in `index-v2.html` so the two closing paragraphs ("隨著總預算付委..." / "115年度總預算正在審議...") render before both buttons, with both buttons positioned at the very bottom of the section. Verify the rendered DOM order is: paragraph 1, paragraph 2, button 1, button 2.

## 6. 報導頁：第四章行動裝置溢位修正

- [x] 6.1 Apply the "`.val-num` mobile overflow fix: allow wrapping instead of forcing single-line width past viewport" decision in `index-v2.html`: remove `white-space: nowrap` from `.val-num` inside the existing `@media (max-width: 968px)` block (desktop's `.val-num` keeps `white-space: nowrap` unchanged), across every `.pyramid-align-row .bar-group.right-bar-box .val-num` instance in Chapter 4. Verify at a mobile viewport (390-430px wide) that `document.body.scrollWidth` no longer exceeds `document.documentElement.clientWidth` (allowing for a few px of normal rounding), using the same `getBoundingClientRect`-based overflow scan used during discussion, checked against every `.pyramid-align-row` instance on the page, not just the one already found.

## 7. 資料庫頁：篩選列 UI

- [x] 7.1 Remove the "第一步："/"第二步："/"第三步：" label prefixes from the three filter section headers in `database-v2.html`, keeping the icon and the rest of each label (輸入關鍵字 / 按主題篩選 / 按相關部會篩選). Verify the rendered labels no longer contain "第N步：" text.
- [x] 7.2 Implement "Desktop pill-row scroll uses pointer-based drag-to-scroll" in `database-v2.html`, satisfying the "Desktop users can reach hidden pills via drag-to-scroll" requirement: add a `pointerdown`/`pointermove`/`pointerup` drag handler to `.pill-row` elements (`#theme-pills`, `#ministry-pills`) that scrolls the row proportionally to horizontal pointer movement once movement exceeds a small threshold (~5px), while a plain click-without-movement still reaches the underlying `.pill-btn` click handler, and an interrupted drag (`pointerleave` before `pointerup`) stops tracking cleanly. Verify by simulating a mousedown-move-up sequence on a pill row wider than its container and confirming `scrollLeft` changes accordingly, that a zero-movement click still selects a pill, and that the existing `fade-left`/`fade-right` classes keep updating during the drag.
- [x] 7.3 Remove the `#summary-amount` total-amount display in `database-v2.html` that currently sits next to "符合條件共 XX 筆計畫", keeping only the count text. Verify the rendered summary bar shows the count text with no adjacent amount value.

## 8. 資料庫頁：關鍵字標籤

- [x] 8.1 Implement "Keyword tags render from the existing `關鍵字` field, chip-styled like `.pill-btn`" in `database-v2.html`, satisfying the "Project cards display keyword tags from spreadsheet data" requirement: for each rendered `.project-block` card, split that project's `關鍵字` field on `、`, trim each segment, and render one `<span class="pb-tag">` chip per non-empty segment (zero chips if the field is empty/missing), sourced from the existing live spreadsheet fetch only — no hardcoded tag data. Style `.pb-tag` as a non-interactive, smaller/lighter variant of `.pill-btn` (same pill shape and hairline border, no hover/active/selected states, no click handler). Verify against the example table (`交通設備` → 1 chip; `文化、大眾傳播、新聞聯繫` → 3 chips; empty/missing → 0 chips) and confirm clicking a chip does not trigger any filter action.

## 9. 驗證與收尾

- [x] 9.1 Re-verify the mobile header hamburger menu (資料庫/接頁 buttons) on `index-v2.html` at a real or emulated mobile viewport, confirming all three nav links (年度深度報導 / 完整資料庫 / 總預算案專頁) render correctly after the nav-order swap in task 2.1 — this is a re-verification only, since the underlying toggle mechanism was already confirmed working during discussion; no code fix is expected unless a regression is found.
- [x] 9.2 Confirm the original files are untouched: run `git diff -- 115MoneyDemoB-main/index.html 115MoneyDemoB-main/database.html` and verify it shows no changes, proving all fixes landed only in `index-v2.html`/`database-v2.html`.
