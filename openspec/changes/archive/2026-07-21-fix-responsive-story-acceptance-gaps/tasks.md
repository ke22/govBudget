## 1. Hero stamp / title collision (Overdue stamp position is computed relative to rendered title bounds)

- [x] 1.1 Implement "Overdue stamp position is computed relative to rendered title bounds": compute the stamp's worst-case rendered footprint at each of the ≤1024px breakpoints (compact and intermediate) and reserve a matching clearance margin on `.hero-headline-panel`/`.hero-title` per the "Hero stamp: compute position from title geometry, not a second independent rule" decision. Verify by reading the computed `top`/`margin` values and confirming the reserved margin is at least the stamp's rendered height minus its `top` offset at each breakpoint.
- [x] 1.2 Verify the overdue stamp's and hero title's rendered bounding boxes do not intersect for the current title/subtitle copy at 320x568, 390x844, 430x932, 768x1024, 820x1180, and 1024x768 CSS pixels, satisfying "Stamp and title never intersect across the supported width matrix" and "A narrower phone width is also collision-free". Verification: `getBoundingClientRect()` comparison at each viewport shows zero intersection.
- [x] 1.3 Confirm the reserved-margin mechanism is driven by the title's actual breakpoint-bound font size/line count (not a value hand-tuned to today's copy alone), satisfying "Independent edits to either element cannot silently reintroduce overlap". Verification: change the title copy to a two-line-forcing string locally, re-check 1.2's viewports still show zero intersection, then revert.

## 2. Chapter-2 Gantt frame at intermediate width (Chart frame geometry resizes consistently at every breakpoint)

- [x] 2.1 Implement "Chart frame geometry resizes consistently at every breakpoint": add `left`/`right`/`bottom` overrides for `.gantt-rows-container-rebuilt`, `.gantt-grid-rebuilt`, `.gantt-x-axis-rebuilt`, and `.gantt-red-deadline-stage` inside the existing `@media (min-width: 641px) and (max-width: 1024px)` block (index-v2.html:3228), sized proportionally for that width range, per the "Gantt frame: extend the existing intermediate-breakpoint block, not a new one" decision. Verification: all four selectors' `left`/`right`/`bottom` values are present and identical to each other inside that media block.
- [x] 2.2 Verify every Chapter-2 Gantt bar renders fully inside the chart panel's bounds and no axis tick or label renders outside the panel at 768x1024 and 1024x768 CSS pixels, satisfying "Intermediate width keeps the Chapter-2 Gantt contained". Verification: `getBoundingClientRect()` of each `.gantt-bar-rebuilt` and axis tick is fully within `.gantt-backdrop-box-rebuilt`'s bounds at both viewports.
- [x] 2.3 Confirm the intermediate override touches the complete frame-element set together (not a partial subset), satisfying "Adding an intermediate override does not require touching every element by hand". Verification: source review of the added media-query block confirms all four selectors from 2.1 share the same coordinate values.

## 3. Chapter-2 step-9 image/card sequencing (Final history image completes before the final story card enters)

- [x] 3.1 Implement "Final history image completes before the final story card enters": add a `transitionend` listener on the third `.history-img-frame` that, once its opacity transition to 1 completes, adds the class/style that allows the step-9 story card's entrance transition to begin, per the "Card/image sequencing: `transitionend`-driven, not a fixed timer" decision. Verification: source review confirms the card's entrance-triggering class/style is applied only inside the `transitionend` callback, not directly inside `updateChapter2Stage()`'s step-9 branch.
- [x] 3.2 Verify at desktop width that scrolling forward to the Chapter-2 step-9 threshold shows the third history image reach full opacity before the step-9 story card's entrance transition visibly begins, satisfying "Image finishes before card enters". Verification: a scroll-through capture (or computed-style timeline read) shows the image's opacity reaching 1 strictly before the card's transform/opacity begins changing from its resting "hidden" state.
- [x] 3.3 Implement the failure-mode fallback: if the `transitionend` event does not fire within a bounded cap, the step-9 card SHALL still become interactive rather than remaining permanently blocked, per the Implementation Contract's failure-mode requirement. Verification: manually suppress/skip the `transitionend` event once in a local test and confirm the card still becomes reachable/interactive.
- [x] 3.4 Confirm `prefers-reduced-motion: reduce` skips the sequencing delay and shows both the image and the card immediately, consistent with the already-archived `responsive-storytelling-motion` spec's reduced-motion requirement. Verification: with reduced motion enabled, both the third image and the step-9 card are visible immediately on reaching step 9, with no observable sequencing delay.

## 4. Chapter-2 → Chapter-3 exit guard extension (Exit state is reversible and motion-aware; Text story-card is covered by the same exit guard as the image backdrop)

- [x] 4.1 Implement the "Exit state is reversible and motion-aware" extension: apply `updateHistoryExit()`'s already-computed `progress` value (and its `chapter3Top <= viewportHeight` hard-exit check) as a second opacity/`pointer-events` target on the step-9 story-card element itself, per the "Exit guard extension: reuse `updateHistoryExit()`'s computed progress, applied to a second target" decision. Verification: source review confirms the step-9 card's opacity/`pointer-events` are driven from the same `progress` variable as `#backdrop-images-rebuilt`, not a separately computed value.
- [x] 4.2 Verify scrolling forward and then backward through the Chapter 2 → Chapter 3 boundary at 390x844, 768x1024, and 1366x768 CSS pixels shows no frame where the step-9 story card or the history-image backdrop visually overlaps Chapter 3 heading/chart content, in either direction, satisfying "Text story-card is covered by the same exit guard as the image backdrop". Verification: forward-then-backward scroll capture at each viewport shows zero overlapping frames for both elements.
- [x] 4.3 Verify the reverse-scroll restoration behavior described in "Reader reverses into Chapter 2" and the reduced-motion behavior in "Reduced-motion exit" both still hold after 4.1's change (no regression). Verification: re-run both scenarios' existing checks — scrolling upward from Chapter 3 restores the history visual's state from current card position with no stale forward-scroll state, and reduced-motion mode switches without an opacity transition.

## 5. Cross-cutting regression check

- [x] 5.1 Re-run the seven-viewport matrix (390x844, 430x932, 768x1024, 820x1180, 1024x768, 1366x768, 1440x900) and the forward/reverse Hero-to-Chapter-3 scroll check already used for `stabilize-responsive-story-layout` (see its archived `tasks.md` section 5), confirming all five Success Criteria in `proposal.md` pass with no new overlap, clipping, or horizontal overflow introduced. Verification: DOM overflow checks and scroll-through capture at all seven viewports show no regression relative to the archived baseline.
