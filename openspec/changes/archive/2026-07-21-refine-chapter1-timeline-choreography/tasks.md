## 1. Timeline card reveal leads the growing line by a fixed offset

- [x] 1.1 Implement "Timeline card reveal leads the growing line by a fixed offset" per the "40px line-lead: offset the card's own reveal threshold, not the line's fill formula" decision: add a fixed 40px to each `.timeline-row.item-card`'s `cardOffset` before it is converted into `revealAt` inside `updateTimelineMainLineFill()`, leaving the line's own `--timeline-progress` fill formula unchanged. Verification: source review confirms the 40px addition is applied only to the per-card threshold, not to the line-fill percent calculation.
- [x] 1.2 Verify at 1366x768 and 1024x768 CSS pixels that forward scrolling reveals each timeline card only after the rendered line has visually passed 40px beyond that card's position, satisfying "Card reveals after the line has passed its position". Verification: scroll-through capture (or computed `--timeline-progress`/`getBoundingClientRect()` comparison) shows the card's `.timeline-card-revealed` class is added strictly after the line-fill's rendered edge passes the 40px-adjusted threshold, not simultaneously with it.
- [x] 1.3 Verify reverse scrolling un-reveals each card symmetrically with no stale state, satisfying "Reverse scroll un-reveals symmetrically". Verification: scrolling upward past a card's lead-adjusted position removes `.timeline-card-revealed` from that card, recomputed fresh each frame with no leftover class from the forward pass.

## 2. Timeline terminal marker reflects completion state

- [x] 2.1 Implement "Timeline terminal marker reflects completion state" per the "Terminal marker: single element with two CSS states, driven by fill percent, not a fixed scroll event" decision: add a new terminal-marker element at the end of `.center-main-line`, styled with a pulsing "?" state by default and an arrow state gated on a class (e.g. `.timeline-complete`) toggled from `updateTimelineMainLineFill()` using the same `--timeline-progress` value already computed for the line. Verification: source review confirms the marker's complete/incomplete class is set from the existing progress variable, not a separately computed value.
- [x] 2.2 Verify "Incomplete timeline shows a pulsing question mark" and "Completed timeline shows an arrow": at 1366x768, scrolling forward shows the pulsing "?" state while progress is below 100% and the arrow state only once the last `.timeline-row.item-node` is fully revealed. Verification: computed-class read (or scroll-through capture) at progress < 100% and at progress = 100% shows the expected marker state in each case.
- [x] 2.3 Verify "Scrolling back up reverts the completed marker": scrolling upward from the completed state drops progress below 100% and the marker reverts from arrow back to the pulsing "?" mark. Verification: scroll-up capture (or class-state read) immediately after crossing back below the 100% threshold shows the reverted state.

## 3. Intermediate breakpoint: 641-1024px timeline reveal

- [x] 3.1 Implement the "Intermediate breakpoint: re-scope compact stacking to ≤640px and add a dedicated 641-1024px layout, not widen the desktop block directly" decision, part one: narrow the existing single-column stacking `@media (max-width: 1024px)` block to `@media (max-width: 640px)` so it no longer applies to the 641-1024px range. Verification: source review confirms the media query boundary is changed from `1024px` to `640px` and no other rule inside that block is altered.
- [x] 3.2 Implement the same decision, part two: widen the timeline reveal's CSS gate from `@media (min-width: 1025px)` and its JS condition from `window.innerWidth >= 1025` down to `641px`, and add a new `@media (min-width: 641px) and (max-width: 1024px)` block with its own alternating-card layout for `.timeline-row.item-card`, satisfying the MODIFIED "Storytelling layout and motion use three tiers" requirement's "Intermediate storytelling simplifies motion, except Chapter 1's timeline" scenario. Verification: source review confirms both the CSS media query and the JS `innerWidth` check use `641` instead of `1025`, and the new intermediate block defines its own left/right alternation distinct from both the ≤640px stacked layout and the ≥1025px desktop layout.
- [x] 3.3 Verify at 768x1024 and 1024x768 CSS pixels that the timeline's progressive card reveal, 40px line-lead, and terminal marker (from tasks 1 and 2) are all active exactly as they are at desktop width, and that cards alternate left/right rather than single-column stacking. Verification: scroll-through capture at both viewports shows `.timeline-card-revealed` toggling correctly and alternating card positions, matching desktop behavior.
- [x] 3.4 Verify at 390x844 and 430x932 CSS pixels (≤640px, phone) that the timeline remains single-column and non-progressive exactly as before this change, with no regression from the narrowed stacking block in task 3.1. Verification: scroll-through capture at both viewports shows no `.timeline-card-revealed` toggling and single-column stacked layout, unchanged from the pre-change baseline.

## 4. Cross-cutting regression check

- [x] 4.1 Re-run the seven-viewport matrix (390x844, 430x932, 768x1024, 820x1180, 1024x768, 1366x768, 1440x900) forward/reverse scroll check already used for `stabilize-responsive-story-layout` and `fix-responsive-story-acceptance-gaps`, confirming no new overlap, clipping, or horizontal overflow was introduced by this change's CSS/JS edits to `.center-timeline-container`. Verification: DOM overflow checks and scroll-through capture at all seven viewports show no regression relative to the prior baseline.
